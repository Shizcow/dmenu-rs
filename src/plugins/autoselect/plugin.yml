about: |
  Auto select if there is only one match with the current input
  Pass --autoselect to enable
entry: main.rs # entry has all overriding and pub items

args:
  - autoselect:
      help:  Auto select if there is only one match with the current input
      long:  autoselect
      # short: 
